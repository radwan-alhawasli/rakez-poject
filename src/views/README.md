# Legacy view folder

This directory is **not** used by the current router: routes import from `@/modules/<domain>/views/`. See [docs/VIEW_PLACEMENT.md](../../docs/VIEW_PLACEMENT.md) for where to add or edit screens.
