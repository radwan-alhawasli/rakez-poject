Technical debt backlog toward 10/10 quality (post-refactor sweep March 2026)

A) Files still over 500 lines (eslint max-lines is error at 500 with skipBlankLines/skipComments; documented eslint-disable max-lines only in: EditorView.vue, useCreditBookings.js, EditContractInfoModal.vue — split remaining large files gradually)
   - src/services/pdfService.js — already modular under src/services/pdf/ (facade re-exports).
   - src/services/hrService.js — facade; implementation under src/services/hr/ (watch hrOperations.js ~450 lines).
   - src/services/salesService.js — facade composing salesServiceCoreMethods + salesServiceExtendedMethods + salesNegotiationsApi under src/services/sales/.
   - src/services/contractService.js, creditService.js, accountingService.js, marketingService.js
   - Large Vue SFCs: SoldUnitDetailView.vue, EditContractInfoModal.vue, UserManagement.vue, ContractFormView.vue, EditorView.vue, EditorProjectsView.vue, ReservationsView.vue, TasksView.vue, MarketingProjectsTab.vue — extract template sections/modals or move logic deeper into existing composables.

B) Security / UX
   - Audit every v-html / API-sourced HTML against sanitizer usage.
   - Confirm token/storage strategy (secureStorage) against XSS and session fixation assumptions.

C) Types & contracts
   - Prefer JSDoc @typedef + // @ts-check on critical services (e.g. accountingService.js for sold-units). Full .ts migration remains optional for new code only.

D) Tests
   - Raise Vitest coverage on extracted composables (useMainLayout, split marketing tabs).

E) Performance
   - Lazy-load heavy modals/tabs with defineAsyncComponent where bundle analysis shows wins.

F) Structure (done)
   - Canonical path: src/modules/<domain>/views only; no parallel src/views. Keep new routes aligned with viewDomainMap.js.
