Technical debt backlog toward 10/10 quality (post-refactor sweep March 2026)

A) Files still over 500 lines (eslint max-lines is error at 500 with skipBlankLines/skipComments; documented eslint-disable max-lines only in: EditorView.vue, useCreditBookings.js, EditContractInfoModal.vue — split remaining large files gradually)
   - src/services/pdfService.js — already modular under src/services/pdf/ (facade re-exports).
   - src/services/hrService.js — facade; implementation under src/services/hr/ (watch hrOperations.js ~450 lines).
   - src/services/salesService.js — facade composing salesServiceCoreMethods + salesServiceExtendedMethods + salesNegotiationsApi under src/services/sales/.
   - contractService.js — split into src/services/contracts/* + facade (same pattern as sales). Remaining: creditService.js, accountingService.js, marketingService.js
   - Large Vue SFCs: SoldUnitDetailView.vue, EditContractInfoModal.vue, UserManagement.vue, ContractFormView.vue, EditorView.vue, EditorProjectsView.vue, ReservationsView.vue, TasksView.vue, MarketingProjectsTab.vue — extract template sections/modals or move logic deeper into existing composables.

B) Security / UX
   - Source of truth: docs/SECURITY.md (v-html / innerHTML inventory, safeHtml.js, CSRF, localStorage & session assumptions). Keep the table updated when adding HTML rendering paths.

C) Types & contracts
   - CI gate: `npm run typecheck` (tsconfig.checkjs.json on src/api + src/services; strict checkJs with noImplicitAny + useUnknownInCatchVariables; see docs/TYPECHECK_SCOPE.md and src/utils/caughtError.js). Prefer JSDoc @typedef for new surface area. Full .ts migration remains optional for new code only.

D) Tests
   - Raise Vitest coverage on extracted composables (useMainLayout, split marketing tabs). Inventory + gate: docs/CRITICAL_TEST_COVERAGE.md; CI: `npm run test:coverage:critical` (vitest.coverage-critical.config.js).

E) Performance
   - Bundle budgets: `npm run build:check-budget` + docs/PERFORMANCE_BUDGETS.md + scripts/performance-budgets.json (gzip limits per vendor chunk). Lazy-load heavy modals/tabs with defineAsyncComponent / dynamic import where analysis shows wins; align increases with budget updates.

F) Structure (done)
   - Canonical path: src/modules/<domain>/views only; no parallel src/views. Keep new routes aligned with viewDomainMap.js.
